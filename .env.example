# Frontend Configuration
# Copy this file to .env and fill in your actual values

# Backend API URL (where the secure proxy is running)
# Use your VM IP address
REACT_APP_BACKEND_URL=http://192.168.146.131:3001

# Optional: Default project key to filter by (leave empty for all projects)
REACT_APP_DEFAULT_PROJECT=

# NOTE: Jira credentials are now stored securely in the backend
# See backend/.env.example for Jira configuration
